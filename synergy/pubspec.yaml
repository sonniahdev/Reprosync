name: synergy
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.8.0-91.0.dev

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.14.0 # Updated to resolve conflict
  firebase_database: ^11.0.0
  firebase_auth: ^5.3.0
  webview_flutter: ^4.0.0
  cloud_firestore: ^5.4.1
  cupertino_icons: ^1.0.8
  url_launcher: ^6.2.1
  http: ^1.2.2
  intl: ^0.20.2
  image_picker: ^1.1.2
  firebase_storage: ^12.4.8
  fl_chart: ^1.0.0
  sqflite: ^2.4.2
  shared_preferences: ^2.5.3
  provider: ^6.1.5
  jwt_decoder: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/fonts/ # For 'SF Pro Display' (ensure font files are present)

